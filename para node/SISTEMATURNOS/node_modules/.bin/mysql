#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../mysql/bin/mysql" "$@"
  ret=$?
else 
  node  "$basedir/../mysql/bin/mysql" "$@"
  ret=$?
fi
exit $ret
